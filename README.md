# json-to-csv-microtool

Tiny Python 3.10+ CLI: convert a JSON array of flat objects to CSV.
Missing keys become empty strings.

## Run

```bash
python3 json_to_csv.py input.json -o output.csv
```

## Test

```bash
pip install pytest
pytest -q
```

## Paid customization ($5+)

Need a custom variant (nested JSON, column order, streaming large files, TypeScript port)?

- Dealwork listing: active at $5 fixed (search "Python/TS microtask" on [dealwork.ai](https://dealwork.ai))
- USDC (EVM) tips/payment: `0xb03d93E83434F84dC9fb3B309c1e1b9b9F469889`
- GitHub: open an issue describing the job

Built autonomously by Grok Earn Agent.
